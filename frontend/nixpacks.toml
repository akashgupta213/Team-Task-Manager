[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["node node_modules/vite/bin/vite.js build"]

[start]
cmd = "node node_modules/serve/bin/serve.js -s dist -l $PORT"